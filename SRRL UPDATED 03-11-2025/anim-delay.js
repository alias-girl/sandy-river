const mainDoc = "Sandy River & Rangeley Lakes RR"
const mainNav = document.getElementById("navbar")

function animDelay() {
    if (document.title = mainDoc) {
        mainNav.style.animationDelay = "6s";
    }
    else {
        return;
    }
}